
@page "/faq"
<PageTitle>FAQ</PageTitle>
<div class="container py-5">
    <h1 class="text-center mb-4">Frequently Asked Questions</h1>

    <div class="faq-accordion" style="margin-bottom:40px">
        @foreach (var faqItem in FAQContent)
        {
            <div class="faq-item" style="margin-top:5px">
                <button class="faq-question" onclick="toggleAccordion(this)">
                    @faqItem[0]
                    <span class="icon">&plus;</span>
                </button>
                <div class="faq-answer">
                    <p>@faqItem[1]</p>
                </div>
            </div>
        }
    </div>
</div>

@code {
    private List<string[]> FAQContent = new List<string[]>
    {
        new string[] { "Am I allowed to suggest a project as a guest?", "Not at first, but once you have completed the brief account registration to become a volunteer you are allowed to contribute to the suggested projects page by recommending a initiative of your choice." },
        new string[] { "Do I need to register to join or request projects?", "Yes you do. We need to be able to keep track of all join requests and project requests and therefore an account is needed." },
        // Add more FAQ items as needed
        new string[] { "How long does it take for projects to be accepted?", "There are a few factors to consider in this regard including, our operating hours, the amount of requests ahead of your own and the complexity of the requested project. This leaves a variation in response time. We will however do everything we can to make sure that your request is processed as soon as possible. Normally it should not exceed a day or two." },
        new string[] { "How do I get started?", "To get started you will need to register as a volunteer and then you will be able to see all the available projecs you can join and you can even request your own." },
        new string[] { "Is this app free to use?", "Yes, it is completely free to use. However you may need to provide your own supplies for the project you join depending on the nature of the project." },
        new string[] { "What are the benefits of being a volunteer?", "The benefits include, having instant access to all community projects, including a description and the times for the project. You can also suggest a new project if you feel the need to fix something in your community." }
    };
}

<script>

function toggleAccordion(button) {
    const faqItem = button.parentNode;
    const answer = faqItem.querySelector('.faq-answer');

    faqItem.classList.toggle('active');
    answer.style.display = answer.style.display === 'block' ? 'none' : 'block';
}
</script>



<style>

.container {
    max-width: 800px;
    margin: 0 auto;
}

.faq-accordion {
    border-radius: 5px;
    overflow: hidden;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    background-color: #fff;
}

.faq-item {
    border-bottom: 1px solid #ddd;
}

.faq-question {
    width: 100%;
    padding: 15px;
    background-color: #f5f5f5;
    text-align: left;
    border: none;
    outline: none;
    cursor: pointer;
    position: relative;
    font-weight: bold;
}

.faq-answer {
    display: none;
    padding: 10px 15px;
    background-color: #eee;
    font-size: 14px;
}

.faq-answer p {
    margin: 0;
}

.faq-question:focus {
    outline: none;
}

.icon {
    position: absolute;
    right: 15px;
    top: 50%;
    transform: translateY(-50%);
    transition: transform 0.3s ease;
}

.faq-question.active .icon {
    transform: translateY(-50%) rotate(45deg);
}

</style>



